[module/custom_bat]
type = custom/script

format = <label>
;format-background = ${colors.background-transparent}
format-foreground = ${colors.foreground}
;format-underline = ${colors.foreground}
;format-padding = 1

label = %output%

exec = ${POLY_PATH}/poly_battery.sh

interval = 1.0
tail = true
