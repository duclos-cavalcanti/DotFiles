[module/custom_bluetooth]
type = custom/script

format-foreground = ${colors.foreground}
format = <label>

label = %output%

exec = ${POLY_PATH}/poly_bluetooth.sh

click-left = bluetoothctl power on
click-right = bluetoothctl power off

interval = 1.0
tail = true
