[module/root]
type = internal/fs
interval = 25
mount-0 = /
label-background = ${colors.background-transparent}
label-foreground = ${colors.foreground}

label-mounted = "%{T3}ﳎ %{T2} %used%"
label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.foreground-alt}

